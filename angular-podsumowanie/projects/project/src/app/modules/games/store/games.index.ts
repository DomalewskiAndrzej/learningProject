import { createSelector } from '@ngrx/store';
import * as fromGamess from './games-state/games.reducer';
import { getGamessState } from './games-state/games.reducer';

// export const variable = createSelector(
//   getGamessState,
//   (state: fromGamess.gamessState) => state.variable
// );
